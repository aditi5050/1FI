# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/1fi-store?retryWrites=true&w=majority

# Base URL for API (used by frontend to call backend routes)
NEXT_PUBLIC_API_BASE=http://localhost:3000/api

# Port (only needed if running Express separately)
PORT=5000
